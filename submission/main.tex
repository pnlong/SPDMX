% SPDMX — ICASSP proceedings draft
% Uses: spconf.sty, IEEEbib.bst
% --------------------------------------------------------------------------
\documentclass{article}
\usepackage{spconf,amsmath,graphicx,hyperref,booktabs}

\title{SPDMX: Public-Domain Multitrack Stem Audio at Scale}

\name{Phillip Long, Eduardo Escoto, Julian McAuley, Zachary Novack}
\address{University of California, San Diego}

\begin{document}
%\ninept
%
\maketitle
%
\begin{abstract}
Recorded multitrack stems remain scarce relative to the data needs of modern separation and generative audio models, while large synthesized alternatives often depend on closed commercial instruments.
We present \textbf{SPDMX}, a public-domain multitrack stem corpus derived from the PDMX symbolic collection.
Scores are cleaned (General MIDI program correction; empty tracks removed) and rendered with open soundfont synthesis and selective MIDI-DDSP for sustained orchestral families.
Render choices are selected with a listening study on perceived realism.
The release comprises approximately $254$K songs and $490$K mono stems at $44.1$\,kHz, sharing song identity with PDMX so symbolic metadata and scores can be used with the audio.
Matched-budget Hybrid Demucs and Stable Audio Open fine-tunes provide proof-of-concept checks against Slakh2100 under objective SI-SDR, FAD, and CLAP.
\end{abstract}
%
\begin{keywords}
Public-domain music, multitrack stems, symbolic-to-audio, source separation, generative audio
\end{keywords}
%
\section{Introduction}
\label{sec:intro}

Music source separation, accompaniment generation, and related audio models consume paired mixtures and stems.
Vision~\cite{deng2009imagenet,schuhmann2022laion}, large-scale text~\cite{raffel2020exploring,gao2020pile}, and broader audio and speech~\cite{gemmeke2017audio,panayotov2015librispeech} enjoy massive public training corpora; music does not.
Commercial recordings exist in abundance, yet copyright largely bars their use as open training data, leaving a scarcity of redistributable, ethically usable audio, especially paired stems.
The open recorded multitracks that do exist, such as MUSDB18~\cite{rafii2019musdb18}, MedleyDB~\cite{bittner2014medleydb}, and MoisesDB~\cite{pereira2023moisesdb}, supply realistic stems but remain small in song count and duration (Table~\ref{tab:datasets}), despite evidence that more training data and augmentation improve separation~\cite{uhlich2017improving,pretet2019singing,stoter2018sigsep}.
Slakh2100~\cite{manilow2019cutting} enlarges training volume by synthesizing MIDI with professional sample libraries, but those virtual instruments are not redistributable, and the release is far smaller than contemporary public-domain symbolic collections.

PDMX~\cite{long2024pdmx} contributes MusicXML and MIDI at public-domain scale.
Naive General MIDI (GM) soundfont rendering of that material yields dry, recognizably synthetic stems; for sustained winds, brass, and strings, short soundfont notes are a particularly weak match to acoustic sustain.
The missing piece is an open path from those scores to stem-level audio that researchers can train on without closed VST stacks.

We introduce \textbf{SPDMX}, a listening-tuned stem release built from PDMX.
Contributions:
(i)~a large public-domain multitrack audio corpus aligned in song identity with PDMX;
(ii)~an open synthesis protocol combining GM register correction, FluidSynth~\cite{fluidsynth} soundfont variety, and selective MIDI-DDSP~\cite{engel2020ddsp,wu2022midi} where soundfonts underserve sustained instruments, with loudness and velocity-aware mixing;
(iii)~an ablation listening study on perceived realism that selects the production render recipe;
(iv)~matched-budget Hybrid Demucs~\cite{defossez2021hybrid} and Stable Audio Open (SAO)~\cite{evans2024stable} proof-of-concept evaluations against Slakh2100.
Upon acceptance, the dataset and all related code will be made publicly available.\footnote{\url{https://pnlong.github.io/SPDMX/}.}

\begin{table}[t]
\centering
\caption{Multitrack and symbolic resources compared to SPDMX.
Hours are mixture or score duration as reported by each source.
``Open synth'' denotes redistributable soundfonts or open neural synthesizers rather than closed commercial VSTs.
Vocals: R$=$recorded singing; S$=$synthesized/choir-style; --$=$none or N/A.}
\label{tab:datasets}
\vspace{1mm}
\setlength{\tabcolsep}{3pt}
\small
\begin{tabular}{@{}lrrlllcc@{}}
\toprule
Dataset & Songs & Hours & Type & Stems & License & MIDI & Voc. \\
\midrule
MUSDB18~\cite{rafii2019musdb18} & 150 & $\sim$10 & Rec. & 4 & Research & No & R \\
MedleyDB~\cite{bittner2014medleydb} & 122 & $\sim$7 & Rec. & var. & Research & No & R \\
MoisesDB~\cite{pereira2023moisesdb} & 240 & $\sim$14 & Rec. & hier. & Research & No & R \\
Slakh2100~\cite{manilow2019cutting} & 2100 & 145 & Synth & 4--48 & Research & Yes & -- \\
PDMX~\cite{long2024pdmx} & $\sim$254k & $\sim$6250 & Symb. & -- & PD & Yes & -- \\
\textbf{SPDMX} & 253982 & 5469 & Synth & var. & Open & Yes & S \\
\bottomrule
\end{tabular}
\end{table}

\section{Related work}
\label{sec:prior}

Open recorded multitracks~\cite{rafii2019musdb18,bittner2014medleydb,pereira2023moisesdb} remain small, and prior large-scale stem synthesis has relied on closed commercial instruments.
Slakh~\cite{manilow2019cutting} is the closest precedent: Lakh MIDI~\cite{raffel2015large,raffel2016learning} rendered with closed commercial sample libraries (VSTs) to study how training-data quantity and quality affect separation.
SPDMX targets the same stem-and-mixture interface from public-domain scores and redistributable synthesizers.

On the synthesis side, MIDI-DDSP~\cite{wu2022midi} (built on DDSP~\cite{engel2020ddsp}; trained on URMP~\cite{li2018creating}) showed that neural MIDI-to-audio can produce high-quality synthetic corpora from symbolic scores, a line continued by the Chamber Ensemble Generator for MIR~\cite{wu2022chamber}.
We adopt that precedent selectively where category-level listening favors it over FluidSynth soundfonts.

\section{SPDMX}
\label{sec:dataset}

PDMX MIDI often carries program numbers that disagree with track names (e.g., a named violin track encoded as piano).
We correct GM programs from track-name aliases before rendering.
Tracks with no note events are dropped so released stems correspond to parts that actually play.
Fig.~\ref{fig:gm_programs} compares program mass before and after correction on non-empty tracks.
After correction, inventory concentrates in acoustic piano, choir/voice, drums, and common orchestral winds, brass, and strings, which motivates concentrating listening effort and neural synthesis on those families.

\begin{figure}[t]
  \centering
  \centerline{\includegraphics[width=\columnwidth]{figs/gm_program_counts_compare.pdf}}
  \caption{Top-10 GM programs by stem share after register correction, compared with raw PDMX program-change shares (non-empty tracks).
  Correcting mislabeled programs and dropping empty tracks reshapes the inventory toward piano, voice, drums, and common orchestral families.}
  \label{fig:gm_programs}
\end{figure}

Each remaining track is rendered to a mono stem at $44.1$\,kHz.
Two soundfont strategies appear in the ablation:
\emph{basic} uses a single default GM bank (\texttt{SGM-V2.01});
\emph{varied} samples a listening-selected shortlist of redistributable soundfonts (and a light FX profile) per instrument category, including \texttt{FluidR3\_GM}, \texttt{GeneralUser GS}, \texttt{Arachno}, and \texttt{Airfont} among others, aiming for Slakh-like timbral variety without proprietary sample libraries.
The complete soundfont inventory is documented with the code and project page.
For eligible monophonic strings, brass, and winds, MIDI-DDSP~\cite{wu2022midi} replaces the soundfont when listening selects a neural path; we fall back to FluidSynth for polyphonic parts, instruments outside the MIDI-DDSP (URMP) inventory, and all other GM programs.
Lyric-conditioned singing-voice synthesis is out of scope; voice-class tracks use choir/voice soundfonts.

Independent per-stem loudness matching would erase relative track levels that MIDI velocities encode.
Each stem is first loudness-normalized toward $-23$\,LUFS~\cite{series2011algorithms} with peak limiting, then scaled by the ratio of that track's maximum note-on velocity to the song-level maximum.
A single anti-clip peak gain, computed on the sum of scaled stems and applied uniformly, keeps stems linearly summable, so the mixture equals the stem sum~\cite{manilow2019cutting}.

On a fixed probe set spanning piano, drums, strings, wind, voice, mallet, organ, guitar, brass, and miscellaneous residual classes, listeners rated blinded render variants for perceived realism while soundfont shortlists and FX profiles were locked per category.
A subsequent formal study compared four non-neural-to-neural render families without audio-to-audio post-enhancement (Sec.~\ref{sec:listening}).
The hybrid recipe frozen from that study (Sec.~\ref{sec:listening}) keeps every song complete under redistributable synthesizers.

SPDMX contains $253{,}982$ songs and $490{,}159$ mono FLAC stems ($5{,}469$ hours of score-aligned material), plus paired MIDI for the released tracks.
Of these, $88{,}403$ songs ($34.8\%$) have more than one stem (mean $1.93$ stems/song; max $66$), so a large multitrack subset sits alongside abundant single-part material.
Songs share identity with PDMX: the same works expose MusicXML, symbolic metadata, and audio stems without a separate alignment procedure.
Unlike Slakh, rendering uses only redistributable open synthesizers rather than closed commercial sample libraries.

\section{Listening study}
\label{sec:listening}

Four render families were compared: basic soundfont, varied soundfonts, MIDI-DDSP over basic fallbacks, and MIDI-DDSP over varied fallbacks.
The open reference was the basic soundfont; when present, the same signal also appeared as a hidden reference among shuffled unlabeled conditions.
Each trial showed a 10-second stem excerpt; listeners scored every condition for perceived realism ($0$--$100$).
The trial set used two stems in each of ten instrument categories ($20$ pages).
We ran the study with $N{=}8$ listeners; Fig.~\ref{fig:ablation_listening} reports realism by instrument category.

We freeze one render path per category from these panels rather than a single global winner.
The default is the highest-mean condition; when the basic soundfont only narrowly outranks varied soundfonts, we keep varied for timbral diversity.
Concretely, SPDMX uses the basic soundfont for drums, organ, and guitar; varied soundfonts for piano, voice, mallet, and miscellaneous residual classes; and MIDI-DDSP over basic fallbacks for strings and brass and varied fallbacks for wind ($\star$ in Fig.~\ref{fig:ablation_listening}).

\begin{figure*}[t]
  \centering
  \centerline{\includegraphics[width=\textwidth]{figs/ablation_listening.pdf}}
  \caption{Ablation listening realism ($0$--$100$) by instrument category for basic, varied soundfonts, and MIDI-DDSP hybrids (basic or varied fallbacks).
  Boxes show per-listener means within each category ($N{=}8$; two stems per category).
  MIDI-DDSP panels are shown only where neural synthesis can apply; elsewhere those conditions would duplicate basic/varied soundfonts.
  Stars mark the condition used in the frozen SPDMX recipe.}
  \label{fig:ablation_listening}
\end{figure*}

\section{Experiments}
\label{sec:experiments}

Two objective proof-of-concept studies use a matched optimizer step budget.
No preference listening is collected for these arms.

\subsection{Source separation}
\label{ssec:separation}

Hybrid Demucs~\cite{defossez2021hybrid} is trained to recover bass, drums, guitar, and piano (Slakh's non-vocal benchmark targets) from coherent mixtures.
For a fair head-to-head with Slakh, we map SPDMX parts onto those four classes and keep only songs that contain all four.
That BDGP-eligible slice is small relative to the full release (roughly $36$\,h of SPDMX training audio versus $83$\,h of Slakh under our manifests), so we do not expect open soundfont/MIDI-DDSP renders to outperform closed-VST Slakh on Slakh2100.
We compare three matched-step arms (Slakh alone, the four-stem SPDMX subset alone, and their union Slakh+SPDMX) as a proof of concept: whether SPDMX stems support standard stem-separation training and can complement Slakh rather than replace it.
In-domain SI-SDR~\cite{leroux2019sdr} on SPDMX (BDGP) shows that the open packs are learnable; the union largely preserves Slakh-test performance while transferring to SPDMX (BDGP), whereas each solo arm fails cross-domain (Fig.~\ref{fig:separation}).
MUSDB18 bass/drums remain difficult for all synth-trained arms.
The remaining multi-stem material (about $88$K songs with two or more stems; orchestral, voice, and other families) is outside this BDGP filter and is the natural substrate for broader open separation settings that are not limited to those four targets.

\begin{figure}[t]
  \centering
  \centerline{\includegraphics[width=\linewidth]{figs/separation_sisdr.pdf}}
  \caption{Hybrid Demucs SI-SDR (dB) under a matched step budget for Slakh, SPDMX, and Slakh+SPDMX training on the BDGP-eligible SPDMX slice versus Slakh, evaluated on Slakh2100, SPDMX (BDGP), and MUSDB18 (bass/drums).}
  \label{fig:separation}
\end{figure}

\subsection{Generative fine-tuning}
\label{ssec:sao}

Unlike Slakh2100, which is organized around separation targets, SPDMX is primarily a large public-domain \emph{audio} corpus with paired MIDI: mixtures support generative fine-tuning and related tasks even for single-stem songs or songs outside the four-stem filter above.
SAO~1.0~\cite{evans2024stable} is fine-tuned from the public checkpoint on loudness-normalized song mixtures with simple instrumental captions under three arms: Slakh train, the same four-stem SPDMX songs used for separation, and full SPDMX.
Held-out generations for a fixed prompt set are scored with OpenL3-based Fr\'{e}chet audio distance~\cite{kilgour2018fr,cramer2019look} and LAION-CLAP~\cite{wu2023large} prompt adherence (Fig.~\ref{fig:sao}).

\begin{figure}[t]
  \centering
  \centerline{\includegraphics[width=\linewidth]{figs/sao_metrics.pdf}}
  \caption{Stable Audio Open fine-tunes under a matched step budget: OpenL3 FAD (lower better) and LAION-CLAP (higher better) by training arm.
  \textit{Bars update when evaluation completes.}}
  \label{fig:sao}
\end{figure}

\section{Conclusion}
\label{sec:conclusion}

SPDMX converts public-domain PDMX scores into open multitrack stem audio at the scale of the parent symbolic corpus, using listening-selected FluidSynth and MIDI-DDSP rendering with loudness- and velocity-aware mixing.
Ablation listening supports a hybrid production recipe, and proof-of-concept Demucs and SAO fine-tunes situate the release relative to Slakh2100 under SI-SDR, FAD, and CLAP.
Broader listener pools and stronger generative evaluations remain open.

\vfill\pagebreak

\section{REFERENCES}
\label{sec:refs}
\bibliographystyle{IEEEbib}
\bibliography{main}

\end{document}

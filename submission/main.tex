% sPDMX — ICASSP proceedings draft
% Uses: spconf.sty, IEEEbib.bst
% --------------------------------------------------------------------------
\documentclass{article}
\usepackage{spconf,amsmath,graphicx,hyperref,booktabs}

\title{sPDMX: Public-Domain Multitrack Stem Audio at Scale}

\name{Phillip Long, Eduardo Escoto, Julian McAuley, Zachary Novack}
\address{University of California, San Diego}

\begin{document}
%\ninept
%
\maketitle
%
\begin{abstract}
Recorded multitrack stems remain scarce relative to the data needs of modern separation and generative audio models, while large synthesized alternatives often depend on closed commercial instruments.
We present \textbf{sPDMX}, a public-domain multitrack stem corpus derived from the PDMX symbolic collection.
Scores are cleaned (General MIDI program correction; empty tracks removed), rendered with open soundfont synthesis and selective MIDI-DDSP for sustained orchestral families, and mixed with per-stem loudness normalization plus MIDI-velocity relative scaling so inter-track dynamics survive loudness matching.
Render choices are selected with a dual-scale listening study (content adherence and realism).
The release comprises approximately $254$K songs and $490$K mono stems at $44.1$\,kHz, sharing song identity with PDMX so symbolic metadata and scores can be used with the audio.
Matched-budget Hybrid Demucs and Stable Audio Open fine-tunes compare Slakh2100, hour-matched sPDMX, and full sPDMX under objective SI-SDR, FAD, and CLAP.
\end{abstract}
%
\begin{keywords}
Public-domain music, multitrack stems, symbolic-to-audio, source separation, generative audio
\end{keywords}
%
\section{Introduction}
\label{sec:intro}

Music source separation, accompaniment generation, and related audio models consume paired mixtures and stems.\footnote{Audio examples are available on the project demo page.}
Recorded corpora such as MUSDB18~\cite{rafii2019musdb18}, MedleyDB~\cite{bittner2014medleydb}, and MoisesDB~\cite{pereira2023moisesdb} supply realistic multitracks, yet remain small in song count and duration (Table~\ref{tab:datasets}).
Slakh2100~\cite{manilow2019cutting} enlarges training volume by synthesizing MIDI with professional sample libraries, but those virtual instruments are not redistributable, and the release is far smaller than contemporary public-domain symbolic collections.

PDMX~\cite{long2024pdmx} contributes MusicXML and MIDI at public-domain scale.
Naive General MIDI (GM) soundfont rendering of that material yields dry, recognizably synthetic stems; for sustained winds, brass, and strings, short soundfont notes are a particularly weak match to acoustic sustain.
The missing piece is an open path from those scores to stem-level audio that researchers can train on without closed VST stacks.

We introduce \textbf{sPDMX}, a listening-tuned stem release built from PDMX.
Contributions:
(i)~a large public-domain multitrack audio corpus aligned in song identity with PDMX;
(ii)~an open synthesis protocol combining GM register correction, FluidSynth~\cite{fluidsynth} soundfont variety, and selective MIDI-DDSP~\cite{engel2020ddsp,wu2022midi} where soundfonts underserve sustained instruments, with loudness and velocity-aware mixing;
(iii)~a dual-scale ablation listening study that selects the production render recipe;
(iv)~matched-budget Hybrid Demucs~\cite{defossez2021hybrid} and Stable Audio Open (SAO)~\cite{evans2024stable} proof-of-concept evaluations against Slakh2100.

\begin{table}[t]
\centering
\caption{Multitrack and symbolic resources compared to sPDMX.
Hours are mixture or score duration as reported by each source.
``Open synth'' denotes redistributable soundfonts or open neural synthesizers rather than closed commercial VSTs.
Vocals: R$=$recorded singing; S$=$synthesized/choir-style; --$=$none or N/A.}
\label{tab:datasets}
\vspace{1mm}
\setlength{\tabcolsep}{3pt}
\small
\begin{tabular}{@{}lrrlllcc@{}}
\toprule
Dataset & Songs & Hours & Type & Stems & License & MIDI & Voc. \\
\midrule
MUSDB18~\cite{rafii2019musdb18} & 150 & $\sim$10 & Rec. & 4 & Research & No & R \\
MedleyDB~\cite{bittner2014medleydb} & 122 & $\sim$7 & Rec. & var. & Research & No & R \\
MoisesDB~\cite{pereira2023moisesdb} & 240 & $\sim$14 & Rec. & hier. & Research & No & R \\
Slakh2100~\cite{manilow2019cutting} & 2100 & 145 & Synth & 4--48 & Research & Yes & -- \\
PDMX~\cite{long2024pdmx} & $\sim$254k & $\sim$6250 & Symb. & -- & PD & Yes & -- \\
\textbf{sPDMX} & 253982 & $\sim$6250 & Synth & var. & Open & Yes & S \\
\bottomrule
\end{tabular}
\end{table}

\section{Related work}
\label{sec:prior}

Slakh~\cite{manilow2019cutting} is the closest precedent: MIDI from Lakh~\cite{raffel2015large,raffel2016learning} is rendered with professional sample libraries to study how training data quantity and quality affect separation.
That design yields usable synthetic stems at roughly an order of magnitude beyond MUSDB18, but reproduction depends on closed VSTs.
sPDMX targets a similar stem-and-mixture training interface from public-domain scores and redistributable open synthesizers.

Recorded multitrack sets~\cite{rafii2019musdb18,bittner2014medleydb,pereira2023moisesdb} remain the realism reference for evaluation and augmentation studies.
On the synthesis side, DDSP~\cite{engel2020ddsp} and MIDI-DDSP~\cite{wu2022midi} (trained on URMP~\cite{li2018creating}) provide neural MIDI-to-audio for selected instruments; the Chamber Ensemble Generator~\cite{wu2022chamber} scales neural ensemble audio for MIR.
We use MIDI-DDSP only where category-level listening favors it over soundfonts, with FluidSynth covering the remainder of the GM inventory.

\section{sPDMX}
\label{sec:dataset}

\subsection{Score preparation}
\label{ssec:cleaning}

PDMX MIDI often carries program numbers that disagree with track names (e.g., a named violin track encoded as piano).
We correct GM programs from track-name aliases before rendering.
Tracks with no note events are dropped so released stems correspond to sounding parts; dense track indices are remapped accordingly.
Fig.~\ref{fig:gm_programs} compares program mass before and after correction on non-empty tracks.
After correction, inventory concentrates in acoustic piano, choir/voice, drums, and common orchestral winds, brass, and strings, which motivates concentrating listening effort and neural synthesis on those families.

\begin{figure}[t]
  \centering
  \centerline{\includegraphics[width=\columnwidth]{figures/gm_program_counts_compare.pdf}}
  \caption{Top-10 GM programs by stem count after register correction (non-empty tracks), versus raw PDMX program-change shares (percent within each inventory).
  Empty tracks are removed before release.}
  \label{fig:gm_programs}
\end{figure}

\subsection{Open rendering}
\label{ssec:render}

Each sounding track is rendered to a mono stem at $44.1$\,kHz.
Two soundfont strategies appear in the ablation:
\emph{basic} uses a single default GM bank;
\emph{Slakh-style} samples a shortlist of redistributable soundfonts (and a light FX profile) per instrument category, approximating timbral variety without commercial VSTs.
For eligible monophonic strings, brass, and winds, MIDI-DDSP~\cite{wu2022midi} replaces the soundfont when listening selects a neural path; remaining GM programs stay on FluidSynth.
Lyric-conditioned singing-voice synthesis is out of scope; voice-class tracks use choir/voice soundfonts.

\subsection{Mixing}
\label{ssec:mixing}

Independent per-stem loudness matching would erase relative track levels that MIDI velocities encode.
Each stem is first loudness-normalized toward $-23$\,LUFS~\cite{series2011algorithms} with peak limiting, then scaled by the ratio of that track's maximum note-on velocity to the song-level maximum.
A single anti-clip peak gain---computed on the sum of scaled stems and applied uniformly---keeps stems linearly summable, so the mixture equals the stem sum~\cite{manilow2019cutting}.

\subsection{Listening-tuned production recipe}
\label{ssec:recipe}

On a fixed probe set spanning piano, drums, strings, wind, voice, mallet, organ, guitar, brass, and residual polyphonic classes, listeners rated blinded render variants on $1$--$5$ content and realism scales while soundfont shortlists and FX profiles were locked per category.
A subsequent formal study compared four non-neural-to-neural render families without audio-to-audio post-enhancement (Sec.~\ref{sec:listening}).
The frozen release uses a hybrid: Slakh-style soundfonts for piano, voice, and mallet; basic soundfonts for drums, organ, guitar, and residual polyphony; MIDI-DDSP (basic or Slakh-style fallbacks) for strings, brass, and wind.
Where MIDI-DDSP cannot cover a part, the soundfont stem is retained so every song remains complete.

\subsection{Release}
\label{ssec:release}

sPDMX contains $253{,}982$ songs and $490{,}159$ mono FLAC stems (about $6{,}250$ hours of score-aligned material under PDMX duration metadata), plus dense MIDI for the sounding tracks.
Songs share identity with PDMX: the same works expose MusicXML, symbolic metadata, and audio stems without a separate alignment procedure.
Released audio does not include closed commercial instrument libraries.

\section{Listening study}
\label{sec:listening}

Four render families were compared: basic soundfont (A1), Slakh-style soundfonts (B1), MIDI-DDSP over basic fallbacks (CA1), and MIDI-DDSP over Slakh-style fallbacks (CB1).
The open reference was A1; when present, the same signal also appeared as a hidden reference among shuffled unlabeled conditions.
Each trial showed a $\sim$10\,s stem excerpt; listeners scored every condition on content adherence and realism ($0$--$100$).
The trial set used two stems in each of ten instrument categories ($20$ pages).
$N{=}8$ listeners scored the full ablation battery; Fig.~\ref{fig:ablation_listening} reports the four families retained for the release (donor-copy DDSP fallbacks identical to their soundfont donors inherited donor scores at aggregation).

Fig.~\ref{fig:ablation_listening} reports mean scores.
A1 attains the highest means on both scales; CA1 is the strongest neural-augmented trade-off.
Per-category patterns, together with the need for timbral variety on piano- and voice-heavy mass, motivate the hybrid production recipe in Sec.~\ref{ssec:recipe} rather than a single global family.

\begin{figure}[t]
  \centering
  \centerline{\includegraphics[width=\linewidth]{figures/ablation_listening.pdf}}
  \caption{Ablation listening means ($0$--$100$) for content and realism across basic (A1), Slakh-style (B1), and MIDI-DDSP hybrids (CA1, CB1).}
  \label{fig:ablation_listening}
\end{figure}

\section{Experiments}
\label{sec:experiments}

Two objective proof-of-concept studies use a matched optimizer step budget across three training corpora: Slakh2100 train~\cite{manilow2019cutting}, sPDMX hour-matched to that train split, and full sPDMX.
No preference listening is collected for these arms.

\subsection{Source separation}
\label{ssec:separation}

Hybrid Demucs~\cite{defossez2021hybrid} is trained to recover bass, drums, guitar, and piano---Slakh's non-vocal benchmark targets---from coherent mixtures.
sPDMX parts are mapped to those four classes by GM program ranges; songs lacking all four are skipped.
Evaluation reports SI-SDR~\cite{leroux2019sdr} on the Slakh2100 test set (Fig.~\ref{fig:downstream}, left).

\subsection{Generative fine-tuning}
\label{ssec:sao}

SAO~1.0~\cite{evans2024stable} is fine-tuned from the public checkpoint on loudness-normalized mixtures from the same three corpora with simple instrumental captions.
Held-out generations for a fixed prompt set are scored with OpenL3-based Fr\'{e}chet audio distance~\cite{kilgour2018fr,cramer2019look} and LAION-CLAP~\cite{wu2023large} prompt adherence (Fig.~\ref{fig:downstream}, right).

\begin{figure}[t]
  \centering
  \centerline{\includegraphics[width=\linewidth]{figures/downstream_poc.pdf}}
  \caption{Downstream proof-of-concept results under a matched step budget.
  Left: Hybrid Demucs SI-SDR (dB) on Slakh2100 test.
  Right: SAO fine-tunes---FAD (lower better) and CLAP (higher better).
  \textit{Curves/bars update when training and evaluation complete.}}
  \label{fig:downstream}
\end{figure}

\section{Conclusion}
\label{sec:conclusion}

sPDMX converts public-domain PDMX scores into open multitrack stem audio at the scale of the parent symbolic corpus, using listening-selected FluidSynth and MIDI-DDSP rendering with loudness- and velocity-aware mixing.
Ablation listening supports a hybrid production recipe; matched-budget Demucs and SAO runs situate the release relative to Slakh2100 under SI-SDR, FAD, and CLAP.
Broader listener pools and stronger generative evaluations remain open.

\vfill\pagebreak

\section{REFERENCES}
\label{sec:refs}
\bibliographystyle{IEEEbib}
\bibliography{main}

\end{document}
